FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Install serve globally for dev server
RUN npm install -g serve

# Copy source files
COPY . .

# Expose port
EXPOSE 3000

# Default command: watch for changes and serve
CMD ["npm", "run", "watch"]
